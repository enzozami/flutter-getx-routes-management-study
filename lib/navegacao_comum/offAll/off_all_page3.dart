import 'package:flutter/material.dart';

class OffAllPage3 extends StatelessWidget {
  const OffAllPage3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Página 3'),
      ),
      body: Container(),
    );
  }
}
